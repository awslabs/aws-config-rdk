tests:
  - name: my-test-name
    policy:
      file: policy.yaml
      name: my-policy-name-existing
      resource_state: existing
    terraform:
      root: tf-root-module
    pytest:
      options:
        - --cache-clear
      file_or_dir:
        - my-pytest.py